version: '3.8'

services:
  visionguard:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Mount outputs and database to persist them between container restarts
      - ./outputs:/app/outputs
      - ./database:/app/database
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
